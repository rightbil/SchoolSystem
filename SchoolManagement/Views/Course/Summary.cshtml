
@{
    ViewBag.Title = "Summary";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>
<h2>Course Summary</h2>
<script>
    function clearList() {
        $("#divCourse").empty();
    }
    function cancelCallToAction() {

        if (1 == 2) {
            return true;
        } else {
            alert("calls is cancelled");
            return false;
        }
    }
    function countRows() {

        alert(($("#partialViewCourse tr").length - 1) + ' rows received'); // 1 is for the header or th tag
    }
</script>

@*//Mule problem is it is opening in new page (should be in place)*@

@Ajax.ActionLink("All", "All", new AjaxOptions()
{
    HttpMethod = "GET",
    UpdateTargetId = "divCourse",
    InsertionMode = InsertionMode.Replace,
    //LoadingElementId = "divImageLoadingAnimation"
    OnBegin = "clearList",
    //OnBegin="cancelCallToAction"
    OnSuccess = "countRows"
})
<span style="color:blue">|</span>

@Ajax.ActionLink("Top 3", "TopThreeCourses", new AjaxOptions()
{
    HttpMethod = "GET",
    UpdateTargetId = "divCourse",
    InsertionMode = InsertionMode.Replace
})
<span style="color:blue">|</span>

@Ajax.ActionLink("Bottom 3", "BottomThreeCourses", new AjaxOptions()
{
    HttpMethod = "GET",
    UpdateTargetId = "divCourse",
    InsertionMode = InsertionMode.Replace
})

<div id="divImageLoadingAnimation">
    @*//add image tag here*@
</div>
<div id="divCourse">
    
</div>
